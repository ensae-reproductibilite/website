::: {.application}

## Application 9: test unitaire _(optionnel)_

Dans le dossier `tests/`, créer avec l'aide d'une IA assistante (`ChatGPT`, `Claude`, `Copilot`.) un test pour la fonction
`create_pipeline`.

- Effectuer le test unitaire en ligne de commande avec `unittest` (`python -m unittest tests/test_pipeline.py`). Corriger le test unitaire en cas d'erreur.
- Si le temps le permet, proposer des variantes ou d'autres tests.
:::


{{< checkpoint appli9 >}}

